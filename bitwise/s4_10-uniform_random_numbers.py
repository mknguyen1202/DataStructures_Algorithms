'''
Page 36
generate a random i between a and b, inclusive, given a random number generator
that produces zero or one with equal probability. All valeues [a, b] should be equally likely

Hint: How would you mimic a three-sided coin with a two-sided coin?
'''

def uniform_random_bf(lower_bound: int, upper_bound: int) -> int:
    # TODO


def uniform_random(lower_bound: int, upper_bound: int) -> int:
    # TODO
